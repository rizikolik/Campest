<% include ../partials/header %>
<h1><%=campground.name %></h1>

<img src="<%= campground.image %>">

<p><%= campground.description %></p>
<%campground.comments.forEach((comment)=>{ %>
    <p><<i><%=comment.author%></i>-<%=comment.text%></p>
<% }) %>

<% include ../partials/footer %>